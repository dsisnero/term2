# Timeout Worker Demo Example for CML
# Demonstrates a worker that times out if a task takes too long, using CML's timeout event.

require "../src/cml"

# ...existing code for timeout worker demo will be added here...
